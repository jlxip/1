#ifndef IR_H
#define IR_H

/* Just a enum of the productions by name */
typedef enum {
    IR_TOKEN,
    /* --- */
    IR_program,
    IR_uses_rec,
    IR_uses_null,
    IR_use,
    IR_relpath_dot,
    IR_relpath_direct,
    IR_module_dot,
    IR_module_direct,
    IR_globals_rec,
    IR_globals_null,
    IR_global_decl,
    IR_global_func,
    IR_global_struct,
    IR_global_impl,
    IR_global_extern,
    IR_annot_rec,
    IR_annot_null,
    IR_annot,
    IR_annot_args,
    IR_primary_list_rec,
    IR_primary_list_direct,
    IR_function_noargs_void,
    IR_function_noargs_typed,
    IR_function_void,
    IR_function_typed,
    IR_params_rec,
    IR_params_one,
    IR_param_copy,
    IR_param_ref,
    IR_param_mut,
    IR_param_mutref,
    IR_expr_par,
    IR_expr_primary,
    IR_expr_call,
    IR_expression_list_none,
    IR_expression_list,
    IR_expression_list_one,
    IR_expression_list_rec,
    IR_expr_lit,
    IR_lit_bool,
    IR_lit_word,
    IR_lit_float,
    IR_lit_string,
    IR_lit_tuple,
    IR_tuple,
    IR_expr_sizeof,
    IR_expr_hat,
    IR_expr_amp,
    IR_expr_bar,
    IR_expr_star,
    IR_expr_slash,
    IR_expr_plus,
    IR_expr_minus,
    IR_expr_deq,
    IR_expr_neq,
    IR_expr_lt,
    IR_expr_leq,
    IR_expr_gt,
    IR_expr_geq,
    IR_expr_not,
    IR_expr_and,
    IR_expr_or,
    IR_expr_struct_inst,
    IR_struct_inst,
    IR_struct_field_inst_rec,
    IR_struct_field_inst_null,
    IR_expr_assign,
    IR_assign_eq,
    IR_assign_plus,
    IR_assign_minus,
    IR_assign_star,
    IR_assign_slash,
    IR_assign_hat,
    IR_assign_amp,
    IR_assign_bar,
    IR_primary_dot,
    IR_primary_typed,
    IR_primary_id,
    IR_decl_let_id,
    IR_decl_let_p_id,
    IR_decl_let_typed,
    IR_decl_let_p_typed,
    IR_typed,
    IR_type_bool,
    IR_type_byte,
    IR_type_float,
    IR_type_ptr,
    IR_type_string,
    IR_type_word,
    IR_type_direct,
    IR_type_tuple,
    IR_type_tuple_star,
    IR_tupledef_one,
    IR_tupledef_many,
    IR_types_rec,
    IR_types_direct,
    IR_if_only,
    IR_if_elif,
    IR_elif_only,
    IR_elif_rec,
    IR_elif_else,
    IR_else,
    IR_switch,
    IR_switch_rec,
    IR_switch_one,
    IR_switch_case,
    IR_switch_default,
    IR_struct,
    IR_struct_def,
    IR_struct_def_null,
    IR_impl,
    IR_impl_def,
    IR_impl_def_null,
    IR_extern_noargs_void,
    IR_extern_noargs_typed,
    IR_extern_void,
    IR_extern_typed,
    IR_block,
    IR_stmts_rec,
    IR_stmts_null,
    IR_stmt_block,
    IR_stmt_decl,
    IR_stmt_expr,
    IR_stmt_assert,
    IR_stmt_break,
    IR_stmt_continue,
    IR_stmt_fall,
    IR_stmt_ret,
    IR_stmt_retval,
    IR_stmt_if,
    IR_stmt_switch,
    IR_stmt_for,
    IR_stmt_while
} IRType;

#endif
