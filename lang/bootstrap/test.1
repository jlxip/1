extern c_exit(retval: word);

struct Number { a: word, }

impl Number {
    @static
    fn make(n: word) -> @: @ { a = n, };

    fn times(n: word) -> @: @.make(.a * n);
    fn square -> @: .times(.a);
    fn get -> word: .a;
}

fn main {
    c_exit(Number.make(3).times(2).square().get());
}
